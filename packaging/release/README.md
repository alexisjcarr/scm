Release artifacts are built by `./scripts/release.sh <version>`.

The script produces Linux `amd64` and `arm64` tarballs containing:

- all three binaries
- example configs
- systemd units
- example manifests
- the install script
- operator-facing docs
